# 逐句复读播放器（Sentence Repeater）产品需求文档

**目标平台**：Android / iOS（跨平台）  
**核心场景**：语言学习中的精听、跟读、听写训练

---

## 1. 项目背景

现有通用播放器仅支持线性播放与字幕显示，无法满足语言学习中的**单句精听**需求。用户需要一款能根据 SRT 字幕文件，将 MP3 音频按句子切割、支持逐句复读、变速、收藏与进度管理的专用工具。

---

## 2. 用户画像

| 角色 | 特征 | 核心诉求 |
|------|------|----------|
| **家长/教育者** | 为子女准备英语听力材料（如 RAZ、Little Fox、KET 真题），熟悉 Anki 与 PDF 工具链 | 快速导入自制材料，控制播放速度，标记生词句 |
| **自学者** | 使用播客、有声书、考试真题进行精听训练 | 单句循环、AB 复读、变速不降调、后台播放 |
| **教师** | 制作课堂听力素材 | 批量导入、导出学习记录、生成听写文本 |

---

## 3. 功能需求

### 3.1 核心功能（P0 — 必须实现）

#### FR-001 文件导入
- **描述**：用户选择本地 MP3 音频与同名字幕文件（SRT 格式）。
- **输入**：MP3（音频编码不限，单声道/立体声均可）；SRT（UTF-8 编码，支持中文与多行文本）。
- **规则**：
  - 支持手动分两次选择（先音频后字幕）。
  - 支持自动匹配：若同目录下存在同名 `.srt` 文件，自动关联。
  - 支持通过系统分享菜单接收文件（"用逐句复读机打开"）。
- **异常**：字幕解析失败时，提示具体行号与错误内容。

#### FR-002 逐句播放
- **描述**：将音频按 SRT 时间轴切割为独立句子单元。
- **交互**：
  - 点击句子卡片 → 从该句起始时间点开始播放。
  - 播放至该句结束时间自动停止（默认模式）。
  - 播放过程中，当前句子高亮，列表自动滚动居中。
- **边界**：若 SRT 时间戳存在重叠或间隙，以 SRT 为准；间隙期间不自动跳播。

#### FR-003 单句复读（A-B Loop）
- **描述**：对当前句子进行循环播放。
- **交互**：点击复读按钮或双击句子卡片，切换单句循环状态。
- **显示**：复读开启时，当前句子卡片显示循环图标，通知栏同步状态。

#### FR-004 上下句跳转
- **描述**：播放过程中快速切换至前一句或后一句。
- **交互**：物理按键/蓝牙遥控器支持上一曲/下一曲映射为上下句；界面提供显式按钮。
- **规则**：跳转后从目标句起始时间播放。

#### FR-005 变速播放
- **描述**：在不改变音调（Pitch）的前提下调整播放速度。
- **档位**：0.5x / 0.7x / 0.8x / 1.0x / 1.2x / 1.5x / 2.0x。
- **默认**：1.0x。
- **记忆**：当前文件的速度设置应被记忆，下次打开同文件时恢复。

### 3.2 辅助功能（P1 — 重要）

#### FR-006 句子收藏与标记
- **描述**：对句子进行星级标记或加入收藏夹。
- **状态**：未标记 / 生词 / 重点 / 已掌握。
- **数据**：收藏状态持久化，与文件路径哈希绑定。

#### FR-007 生词注释
- **描述**：点击句子中的单词，弹出迷你释义。
- **数据源**：本地离线词典（如 StarDict 格式）或在线接口（有道/金山，需用户授权联网）。
- **显示**：释义以小型标签形式悬浮于单词下方，不遮挡音频控制区。

#### FR-008 听写模式
- **描述**：隐藏句子文本，仅播放音频，用户输入听写内容后对照原文。
- **流程**：播放 → 用户输入 → 提交 → 高亮差异字符。
- **输入**：支持软键盘与蓝牙键盘。

#### FR-009 播放进度与历史
- **描述**：记录每个文件的最近播放句子索引与位置。
- **恢复**：重新打开文件时，询问"从第 X 句继续？"

### 3.3 扩展功能（P2 — 可选）

#### FR-010 后台播放与通知栏控制
- **描述**：应用退至后台或锁屏时，通过系统媒体通知控制播放/暂停/上下句。
- **显示**：通知栏显示当前句子文本（截断至 30 字）。

#### FR-011 批量导入与列表管理
- **描述**：支持导入文件夹，自动识别目录内所有 MP3+SRT 配对，生成课程列表。
- **排序**：按文件名、最近播放、收藏优先。

---

## 4. 非功能需求

### 4.1 性能
- **启动时间**：冷启动至可导入界面 ≤ 2 秒。
- **seek 延迟**：点击句子至音频出声 ≤ 300ms（目标 150ms）。
- **内存占用**：播放 1 小时音频时，应用内存 ≤ 150MB。
- **电量**：后台播放 1 小时耗电 ≤ 3%（屏幕关闭状态下）。

### 4.2 兼容性
- **Android**：最低支持 Android 8.0（API 26），目标 Android 15。
- **iOS**：最低支持 iOS 14.0，目标 iOS 18。
- **音频格式**：必须支持 MP3（CBR/VBR）；可选支持 AAC、WAV、M4A。
- **字幕编码**：必须正确识别 UTF-8；可选兼容 GBK/GB2312（带编码选择提示）。

### 4.3 可靠性与异常处理
- **音频中断**：来电或闹钟打断时，自动暂停并保存当前位置；恢复后询问是否继续。
- **文件丢失**：原文件被删除或移动后，列表项标记为"离线"，不崩溃。
- **解析错误**：SRT 格式错误（如时间戳倒置、空文本）时，跳过该条并提示，不中断整体导入。

### 4.4 安全与隐私
- **权限**：不申请 `READ_EXTERNAL_STORAGE`；使用系统 SAF（Storage Access Framework / UIDocumentPicker）获取持久化 URI。
- **网络**：默认离线工作。仅在用户主动开启"在线查词"时联网。
- **数据**：学习记录本地存储，不上传云端（除非用户主动开启同步）。

---

## 5. 界面与交互规范

### 5.1 主界面结构

```
┌─────────────────────────────┐
│  标题栏（课程名 / 导入按钮）   │
├─────────────────────────────┤
│                             │
│   句子列表（可滚动）         │
│   ┌─────────────────────┐   │
│   │ 🔊 1. Hello world │ ← 当前句高亮，自动居中
│   │    00:00 → 00:03  │   │
│   └─────────────────────┘   │
│   ┌─────────────────────┐   │
│   │   2. How are you?  │   │
│   │    00:04 → 00:06  │   │
│   └─────────────────────┘   │
│                             │
├─────────────────────────────┤
│  [⏮]  [⏯]  [⏭]  [🔁] [⚡] │
│  上一句 播放 下一句 复读 变速 │
├─────────────────────────────┤
│  进度条（当前句内微进度）      │
│  ████████░░░░░░░░░░ 1.2x    │
└─────────────────────────────┘
```

### 5.2 交互细节
- **句子卡片**：
  - 单击：播放该句。
  - 双击：切换该句的复读状态。
  - 长按：弹出菜单（收藏、标记生词、听写模式、复制文本）。
- **进度条**：仅控制**当前句子内**的播放进度，拖动不跨句。
- **空状态**：未导入文件时，中央显示大图标 + "选择 MP3 与字幕"按钮。

### 5.3 主题与无障碍
- **深色模式**：跟随系统，提供纯黑（OLED 省电）与深灰两种深色主题。
- **字体大小**：支持系统字体缩放，最大可放大至 1.5 倍。
- **TalkBack**：句子卡片朗读格式为"第 X 句，[英文文本]，时长 N 秒"。

---

---

## 7. 技术约束与建议

| 约束 | 说明 |
|------|------|
| **跨平台框架** | 推荐 Flutter（`just_audio` + `audio_service`），UI 与逻辑 95% 复用；若拒绝 Dart，可选 KMP（共享逻辑，各自原生 UI + 音频）。 |
| **音频引擎** | 必须支持精确 `seekTo(ms)` 与片段循环（Flutter 用 `ClippingAudioSource`；原生 Android 用 ExoPlayer，iOS 用 AVPlayer）。 |
| **文件系统** | Android 10+ 强制 Scoped Storage，禁止直接文件路径，必须使用 `ContentResolver` / SAF。 |
| **后台保活** | 通过系统媒体播放服务（MediaSession / MPNowPlayingInfoCenter）实现，禁止非必要保活手段。 |

---


## 8. 附录

### A. 参考项目
- PDJ-Media-Repeater（开源复读机，交互参考）
- 海子复读小霸王（安卓端 LRC 复读，功能参考）

### B. 术语表
- **SRT**：SubRip Text，纯文本字幕格式。
- **SAF**：Storage Access Framework，Android 系统级文件选择框架。
- **Pitch  preserved**：变速时保持音调不变（Time-Stretching）。

---
